# UI Tests

Web UI automation is intentionally separated from the Go framework test module.
Recommended default: TypeScript + Playwright.

The Go automation framework can trigger/collect UI runs, but browser test scripts may live here as Playwright specs.
